#include<bits/stdc++.h>
using namespace std;
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl "\n"
#define fraction(a) fixed<<setprecision(a)<<
#define mem(a,b) memset(a,b,sizeof(a))
typedef long long ll;
typedef double dl;


int main()
{
    optimize();
    int t;
    cin >> t;
    while(t--){
        int a , b, c;
        cin >> a >> b >> c;
        cout << 0 << " " << 0 << " " << c << " " << 0 << endl << 0 << " " << 0 << " " << 0 << " " << c << endl;
    }
    return 0;
}

